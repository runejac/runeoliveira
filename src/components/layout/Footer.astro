<footer class="site-footer">
  <div class="footer-inner section-shell">
    <p class="eyebrow">Open to a good conversation</p>
    <div class="footer-callout">
      <h2>Have something<br />worth making?</h2>
      <a href="mailto:runedanielj@gmail.com?subject=Hello%20Rune">
        Let’s talk <span aria-hidden="true">↗</span>
      </a>
    </div>
    <div class="footer-meta">
      <p>© {new Date().getFullYear()} Rune Oliveira</p>
      <nav aria-label="Social links">
        <a
          href="https://github.com/runejac"
          target="_blank"
          rel="noopener noreferrer">GitHub ↗</a
        >
        <a
          href="https://www.linkedin.com/in/rune-daniel-jacobsen-oliveira/"
          target="_blank"
          rel="noopener noreferrer">LinkedIn ↗</a
        >
        <a href="#top">Back to top ↑</a>
      </nav>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    padding-block: clamp(5rem, 10vw, 8rem) 2rem;
    background: var(--accent-soft);
  }

  .footer-callout {
    display: grid;
    padding-block: clamp(2.5rem, 6vw, 5rem);
    border-bottom: 1px solid var(--line);
    align-items: end;
    gap: 2rem;
  }

  .footer-callout h2 {
    font-family: var(--serif);
    font-size: clamp(3.4rem, 8vw, 8rem);
    font-weight: 520;
    letter-spacing: -0.06em;
    line-height: 0.88;
  }

  .footer-callout > a {
    width: max-content;
    padding: 0.8rem 1.1rem;
    border-radius: 999px;
    color: var(--white);
    background: var(--ink);
    font-weight: 700;
    text-decoration: none;
  }

  .footer-meta {
    display: flex;
    padding-top: 1.5rem;
    color: var(--muted);
    flex-direction: column;
    gap: 1rem;
  }

  .footer-meta p,
  .footer-meta a {
    font-family: var(--mono);
    font-size: 0.68rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .footer-meta nav {
    display: flex;
    flex-wrap: wrap;
    gap: 1.25rem;
  }

  .footer-meta a {
    text-decoration: none;
  }

  .footer-meta a:hover {
    color: var(--ink);
  }

  @media (min-width: 48rem) {
    .footer-callout {
      grid-template-columns: 1fr auto;
    }

    .footer-meta {
      flex-direction: row;
      justify-content: space-between;
    }
  }
</style>
