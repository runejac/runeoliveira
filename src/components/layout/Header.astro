<header class="site-header">
  <div class="header-inner">
    <a class="wordmark" href="#top" aria-label="Rune Oliveira, back to top">
      <span class="wordmark-mark" aria-hidden="true">R/O</span>
      <span class="wordmark-name">Rune Oliveira</span>
    </a>

    <nav aria-label="Primary navigation">
      <a href="#work">Work</a>
      <a href="#about">About</a>
      <a href="/resume.pdf" target="_blank" rel="noopener noreferrer">
        Résumé <span aria-hidden="true">↗</span>
      </a>
      <a
        class="contact-link"
        href="mailto:runedanielj@gmail.com?subject=Hello%20Rune">Let’s talk</a
      >
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    z-index: 20;
    top: 0;
    border-bottom: 1px solid var(--line);
    background: rgba(243, 240, 232, 0.88);
    backdrop-filter: blur(18px);
  }

  .header-inner {
    display: flex;
    width: var(--page);
    min-height: 4.5rem;
    margin-inline: auto;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .wordmark {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    text-decoration: none;
  }

  .wordmark-mark {
    display: grid;
    width: 2.4rem;
    height: 2.4rem;
    border-radius: 50%;
    color: var(--white);
    background: var(--ink);
    place-items: center;
    font-family: var(--mono);
    font-size: 0.67rem;
    letter-spacing: -0.05em;
  }

  .wordmark-name {
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  nav {
    display: flex;
    align-items: center;
    gap: clamp(0.8rem, 2vw, 2rem);
  }

  nav a {
    color: var(--muted);
    font-size: 0.9rem;
    text-decoration: none;
    transition: color 160ms ease;
  }

  nav a:hover {
    color: var(--ink);
  }

  nav .contact-link {
    padding: 0.55rem 0.85rem;
    border-radius: 999px;
    color: var(--white);
    background: var(--ink);
    transition:
      color 160ms ease,
      background 160ms ease,
      transform 160ms var(--ease);
  }

  nav .contact-link:hover {
    color: var(--ink);
    background: var(--accent-soft);
    transform: translateY(-1px);
  }

  @media (max-width: 42rem) {
    .header-inner {
      min-height: 4rem;
    }

    .wordmark-name,
    nav a:first-child,
    nav a:nth-child(3) {
      display: none;
    }

    nav {
      gap: 1rem;
    }
  }
</style>
