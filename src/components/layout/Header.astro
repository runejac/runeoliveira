<header class="site-header">
  <div class="header-inner">
    <a class="wordmark" href="#top" aria-label="Rune Oliveira, back to top">
      <span>RO</span><i aria-hidden="true"></i><small>26</small>
    </a>

    <nav aria-label="Primary navigation">
      <a href="#approach"><span>01</span> Method</a>
      <a href="#about"><span>02</span> Story</a>
      <a href="/resume.pdf" target="_blank" rel="noopener noreferrer">
        <span>03</span> Résumé ↗
      </a>
    </nav>

    <a
      class="contact-link"
      href="mailto:runedanielj@gmail.com?subject=Hello%20Rune"
    >
      <span>Start a conversation</span><i aria-hidden="true">↗</i>
    </a>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    z-index: 30;
    top: 0.8rem;
    left: 0;
    width: 100%;
    pointer-events: none;
  }

  .header-inner {
    display: flex;
    width: var(--page);
    min-height: 3.6rem;
    margin-inline: auto;
    padding: 0.45rem 0.5rem 0.45rem 0.9rem;
    border: 1px solid var(--line);
    border-radius: 999px;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    background: rgba(8, 11, 13, 0.75);
    box-shadow: 0 0.5rem 2rem rgba(0, 0, 0, 0.22);
    backdrop-filter: blur(18px);
    pointer-events: auto;
  }

  .wordmark {
    display: flex;
    min-width: 4.2rem;
    align-items: baseline;
    gap: 0.35rem;
    font-family: var(--mono);
    text-decoration: none;
  }

  .wordmark span {
    color: var(--light);
    font-size: 0.8rem;
    letter-spacing: 0.08em;
  }

  .wordmark i {
    width: 0.35rem;
    height: 0.35rem;
    border-radius: 50%;
    background: var(--signal);
    box-shadow: 0 0 0.8rem var(--signal);
  }

  .wordmark small {
    color: var(--muted);
    font-size: 0.58rem;
  }

  nav {
    display: none;
    align-items: center;
    gap: clamp(1.25rem, 3vw, 3rem);
  }

  nav a {
    color: var(--muted);
    font-size: 0.78rem;
    text-decoration: none;
    transition: color 180ms ease;
  }

  nav a span {
    margin-right: 0.3rem;
    color: rgba(184, 255, 88, 0.56);
    font-family: var(--mono);
    font-size: 0.55rem;
  }

  nav a:hover {
    color: var(--light);
  }

  .contact-link {
    display: flex;
    min-height: 2.65rem;
    padding: 0.3rem 0.3rem 0.3rem 1rem;
    border-radius: 999px;
    color: var(--night);
    background: var(--signal);
    align-items: center;
    gap: 0.8rem;
    font-size: 0.78rem;
    font-weight: 700;
    text-decoration: none;
    transition: transform 180ms var(--ease);
  }

  .contact-link i {
    display: grid;
    width: 2.05rem;
    height: 2.05rem;
    border-radius: 50%;
    color: var(--light);
    background: var(--night);
    place-items: center;
    font-style: normal;
  }

  .contact-link:hover {
    transform: translateY(-2px);
  }

  @media (max-width: 29rem) {
    .contact-link span {
      display: none;
    }

    .contact-link {
      padding-left: 0.3rem;
    }
  }

  @media (min-width: 48rem) {
    .site-header {
      top: 1.2rem;
    }

    nav {
      display: flex;
    }
  }
</style>
