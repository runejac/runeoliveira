<section id="approach" class="method">
  <div class="method-inner section-shell">
    <header class="method-heading">
      <div class="section-index">
        <span>01</span>
        <span>Method</span>
      </div>
      <div>
        <p class="eyebrow">What stays constant</p>
        <h2>Good software is a conversation.</h2>
      </div>
      <p class="heading-note">
        Between a person and an interface. Between a surface and its system.
        Between the people making it.
      </p>
    </header>

    <div class="method-grid">
      <article>
        <span>Clarity / 01</span>
        <h3>Useful over flashy.</h3>
        <p>
          Intent should be obvious, feedback immediate, and the clever parts
          quiet enough to stay out of the way.
        </p>
      </article>
      <article>
        <span>Context / 02</span>
        <h3>System to surface.</h3>
        <p>
          I follow product behavior from interface through APIs and data so the
          whole experience tells the same story.
        </p>
      </article>
      <article>
        <span>Momentum / 03</span>
        <h3>Built with others.</h3>
        <p>
          Clear conversation, small iterations, and shared ownership make better
          software than working in isolation.
        </p>
      </article>
    </div>

    <div class="signal-band">
      <p>My working principle</p>
      <strong>Clarity is a feature.</strong>
      <span>Frontend · Product · Systems</span>
    </div>
  </div>
</section>

<style>
  .method {
    position: relative;
    padding-block: clamp(7rem, 14vw, 12rem);
    border-top: 1px solid var(--line);
    background: linear-gradient(
        90deg,
        transparent 49.9%,
        rgba(237, 241, 232, 0.035) 50%,
        transparent 50.1%
      ),
      var(--night-soft);
  }

  .method-heading {
    display: grid;
    margin-bottom: clamp(4rem, 9vw, 8rem);
    gap: 2.5rem;
  }

  .section-index {
    display: flex;
    color: var(--muted);
    flex-direction: column;
    font-family: var(--mono);
    font-size: 0.62rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .section-index span:first-child {
    color: var(--signal);
  }

  .method-heading h2 {
    max-width: 11ch;
    margin-top: 1.5rem;
    font-family: var(--display);
    font-size: clamp(3.6rem, 8vw, 7.7rem);
    font-weight: 300;
    letter-spacing: -0.065em;
    line-height: 0.88;
  }

  .heading-note {
    max-width: 28rem;
    color: var(--muted);
    font-size: 1.08rem;
    align-self: end;
  }

  .method-grid {
    display: grid;
    border-top: 1px solid var(--line);
  }

  .method-grid article {
    position: relative;
    min-height: 20rem;
    padding: 2rem 0;
    overflow: hidden;
    border-bottom: 1px solid var(--line);
  }

  .method-grid article::after {
    position: absolute;
    right: -4rem;
    bottom: -7rem;
    width: 13rem;
    height: 13rem;
    border: 1px solid var(--line);
    border-radius: 50%;
    content: '';
    transition:
      border-color 250ms ease,
      transform 500ms var(--ease);
  }

  .method-grid article:hover::after {
    border-color: var(--signal);
    transform: scale(1.12);
  }

  .method-grid article > span,
  .signal-band p,
  .signal-band span {
    color: var(--muted);
    font-family: var(--mono);
    font-size: 0.62rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .method-grid h3 {
    max-width: 9ch;
    margin-top: 3rem;
    font-family: var(--display);
    font-size: clamp(2rem, 3vw, 3rem);
    font-weight: 300;
    letter-spacing: -0.045em;
    line-height: 1;
  }

  .method-grid p {
    position: relative;
    z-index: 1;
    max-width: 25rem;
    margin-top: 1.25rem;
    color: var(--muted);
  }

  .signal-band {
    display: grid;
    margin-top: clamp(3rem, 7vw, 6rem);
    padding: 1.3rem;
    color: var(--night);
    background: var(--signal);
    align-items: center;
    gap: 1rem;
  }

  .signal-band p,
  .signal-band span {
    color: rgba(8, 11, 13, 0.62);
  }

  .signal-band strong {
    font-family: var(--display);
    font-size: clamp(1.7rem, 3vw, 2.6rem);
    font-weight: 300;
    letter-spacing: -0.04em;
  }

  @media (min-width: 48rem) {
    .method-heading {
      grid-template-columns: 5rem 1fr minmax(16rem, 0.45fr);
    }

    .method-grid {
      grid-template-columns: repeat(3, 1fr);
    }

    .method-grid article {
      padding: 2rem clamp(1.5rem, 3vw, 3rem) 2rem 0;
      border-right: 1px solid var(--line);
    }

    .method-grid article:not(:first-child) {
      padding-left: clamp(1.5rem, 3vw, 3rem);
    }

    .method-grid article:last-child {
      border-right: 0;
    }

    .signal-band {
      grid-template-columns: 1fr 1.5fr 1fr;
      text-align: center;
    }

    .signal-band p {
      text-align: left;
    }

    .signal-band span {
      text-align: right;
    }
  }
</style>
