<section id="about" class="story">
  <div class="story-inner section-shell">
    <header class="story-heading">
      <div class="section-index">
        <span>02</span>
        <span>Story</span>
      </div>
      <div>
        <p class="eyebrow">The route here</p>
        <h2>Unexpected path.<br />Useful perspective.</h2>
      </div>
    </header>

    <div class="story-grid">
      <ol class="timeline" aria-label="Career journey">
        <li>
          <span>Early signal</span>
          <strong>Building computers</strong>
        </li>
        <li>
          <span>Hardware years</span>
          <strong>E-commerce & procurement</strong>
        </li>
        <li>
          <span>Age 29</span>
          <strong>A deliberate change</strong>
        </li>
        <li>
          <span>Now</span>
          <strong>Software & media</strong>
        </li>
      </ol>

      <div class="story-copy">
        <p class="lead">
          I did not grow up knowing I would become a software engineer. I grew
          into it by following the same curiosity from one system to the next.
        </p>
        <p>
          Building computers led to hardware roles in e-commerce and procurement
          for a leading Scandinavian retailer. At 29, I changed direction and
          earned a bachelor’s degree in information technology, specializing in
          frontend and mobile development.
        </p>
        <p>
          That route still shapes how I work: understand the parts, respect the
          person using them, and stay curious about what happens behind the
          interface.
        </p>
        <a
          href="https://www.kristiania.no/studier/bachelor/informasjonsteknologi-frontend-og-mobilutvikling/"
          target="_blank"
          rel="noopener noreferrer"
          >Field of study <span aria-hidden="true">↗</span></a
        >
      </div>
    </div>

    <aside class="off-grid">
      <p>Offline inputs</p>
      <ul>
        <li>Coastline / fly fishing</li>
        <li>Mats / jiu-jitsu</li>
        <li>Tabletop / board games</li>
        <li>Mountains / alpine skiing</li>
      </ul>
    </aside>
  </div>
</section>

<style>
  .story {
    padding-block: clamp(7rem, 14vw, 12rem);
    color: var(--night);
    background: var(--fog);
  }

  .story-heading {
    display: grid;
    margin-bottom: clamp(4rem, 9vw, 8rem);
    gap: 2.5rem;
  }

  .section-index {
    display: flex;
    color: rgba(8, 11, 13, 0.55);
    flex-direction: column;
    font-family: var(--mono);
    font-size: 0.62rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .section-index span:first-child,
  .story .eyebrow {
    color: #4b3fd1;
  }

  .story-heading h2 {
    margin-top: 1.5rem;
    font-family: var(--display);
    font-size: clamp(3.5rem, 8vw, 7.7rem);
    font-weight: 300;
    letter-spacing: -0.067em;
    line-height: 0.87;
  }

  .story-grid {
    display: grid;
    border-top: 1px solid rgba(8, 11, 13, 0.2);
    gap: 3rem;
  }

  .timeline {
    list-style: none;
  }

  .timeline li {
    display: grid;
    padding-block: 1.2rem;
    border-bottom: 1px solid rgba(8, 11, 13, 0.18);
    grid-template-columns: 7rem 1fr;
    gap: 1rem;
  }

  .timeline span,
  .off-grid > p,
  .off-grid li {
    font-family: var(--mono);
    font-size: 0.64rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .timeline span {
    color: rgba(8, 11, 13, 0.5);
  }

  .timeline strong {
    font-size: 0.95rem;
  }

  .story-copy {
    padding-top: 1.2rem;
  }

  .story-copy p {
    max-width: 38rem;
    margin-bottom: 1.2rem;
    color: rgba(8, 11, 13, 0.65);
    font-size: 1.05rem;
  }

  .story-copy .lead {
    color: var(--night);
    font-family: var(--display);
    font-size: clamp(1.8rem, 3.5vw, 2.8rem);
    font-weight: 300;
    letter-spacing: -0.045em;
    line-height: 1.08;
  }

  .story-copy a {
    display: inline-block;
    margin-top: 1rem;
    padding-bottom: 0.25rem;
    border-bottom: 2px solid #4b3fd1;
    font-family: var(--mono);
    font-size: 0.68rem;
    letter-spacing: 0.07em;
    text-decoration: none;
    text-transform: uppercase;
  }

  .off-grid {
    display: grid;
    margin-top: clamp(4rem, 8vw, 7rem);
    padding: 1.25rem 0;
    border-top: 1px solid rgba(8, 11, 13, 0.2);
    border-bottom: 1px solid rgba(8, 11, 13, 0.2);
    gap: 1.5rem;
  }

  .off-grid > p {
    color: #4b3fd1;
  }

  .off-grid ul {
    display: flex;
    list-style: none;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .off-grid li {
    padding: 0.6rem 0.75rem;
    border: 1px solid rgba(8, 11, 13, 0.24);
    border-radius: 999px;
  }

  @media (min-width: 56rem) {
    .story-heading {
      grid-template-columns: 5rem 1fr;
    }

    .story-grid {
      grid-template-columns: minmax(20rem, 0.75fr) minmax(24rem, 1.25fr);
      gap: clamp(3rem, 8vw, 8rem);
    }

    .off-grid {
      grid-template-columns: 10rem 1fr;
      align-items: center;
    }
  }
</style>
