<section id="about" class="about">
  <div class="about-inner section-shell">
    <div class="about-heading">
      <p class="eyebrow">About me</p>
      <h2>
        Hardware first.<br />Software now.<br /><em>Curiosity throughout.</em>
      </h2>
    </div>

    <div class="about-copy">
      <p class="lead">
        My route into software wasn’t a straight line—and that’s a useful part
        of how I work today.
      </p>
      <p>
        I started by building computers as a kid, then worked with hardware in
        e-commerce and procurement for a leading Scandinavian retailer. At 29, I
        changed direction and earned a bachelor’s degree in information
        technology, specializing in frontend and mobile development.
      </p>
      <p>
        Today I care about the full path from an idea to the person using it:
        clear interaction, resilient implementation, and the quiet details that
        make a product feel considered.
      </p>
      <a
        href="https://www.kristiania.no/studier/bachelor/informasjonsteknologi-frontend-og-mobilutvikling/"
        target="_blank"
        rel="noopener noreferrer"
        >My field of study <span aria-hidden="true">↗</span></a
      >
    </div>

    <aside class="off-screen">
      <p>When the screen is off</p>
      <ul>
        <li><span>01</span> Fly fishing by the coast</li>
        <li><span>02</span> Brazilian jiu-jitsu</li>
        <li><span>03</span> Video & board games</li>
        <li><span>04</span> Alpine skiing</li>
      </ul>
    </aside>
  </div>
</section>

<style>
  .about {
    padding-block: clamp(6rem, 12vw, 11rem);
    color: var(--white);
    background: var(--ink);
  }

  .about-inner {
    display: grid;
    gap: clamp(3rem, 7vw, 7rem);
  }

  .about .eyebrow {
    color: rgba(255, 253, 248, 0.58);
  }

  .about-heading h2 {
    max-width: 12ch;
    margin-top: 1.5rem;
    font-family: var(--serif);
    font-size: clamp(3rem, 6.5vw, 6.5rem);
    font-weight: 500;
    letter-spacing: -0.055em;
    line-height: 0.95;
  }

  .about-heading em {
    color: var(--accent);
    font-weight: 420;
  }

  .about-copy {
    max-width: 39rem;
  }

  .about-copy p {
    margin-bottom: 1.25rem;
    color: rgba(255, 253, 248, 0.67);
    font-size: 1.08rem;
  }

  .about-copy .lead {
    color: var(--white);
    font-family: var(--serif);
    font-size: clamp(1.65rem, 3vw, 2.3rem);
    letter-spacing: -0.025em;
    line-height: 1.18;
  }

  .about-copy a {
    display: inline-block;
    margin-top: 0.75rem;
    text-decoration-line: underline;
    text-decoration-color: var(--accent);
    text-decoration-thickness: 2px;
    text-underline-offset: 0.4rem;
  }

  .off-screen {
    padding-top: 1.25rem;
    border-top: 1px solid rgba(255, 253, 248, 0.22);
  }

  .off-screen > p,
  .off-screen span {
    font-family: var(--mono);
    font-size: 0.68rem;
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }

  .off-screen > p {
    margin-bottom: 1.25rem;
    color: var(--accent);
  }

  .off-screen ul {
    list-style: none;
  }

  .off-screen li {
    display: grid;
    padding-block: 0.75rem;
    border-bottom: 1px solid rgba(255, 253, 248, 0.13);
    grid-template-columns: 2.5rem 1fr;
  }

  .off-screen span {
    color: rgba(255, 253, 248, 0.42);
  }

  @media (min-width: 62rem) {
    .about-inner {
      grid-template-columns: 1.05fr 0.75fr;
    }

    .off-screen {
      grid-column: 2;
    }
  }
</style>
